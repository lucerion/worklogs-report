const FIELD_TYPES = {
  date: 'date',
  select: 'select',
  string: 'string',
  text: 'text',
};

const DATE_FORMAT = 'DD/MMM/YYYY';

const TEXT_SEPARATOR = '\n';

export { FIELD_TYPES, DATE_FORMAT, TEXT_SEPARATOR };
